<table class="table">
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td>
          <span><%= image_tag order.item.image, size: "40x40" %></span>
          <span><%= order.item.name %></span>
        </td>
        <td>
          <p><%= corder.item.with_tax_price%></p>
        </td>
        <td>
          <span><%= order.cart_item.amount %></span>
        </td>
        <td>
          <p><%= order.curt_item.subtotal%></p>
        </td>
        <td>
          <p><%= link_to "削除する", cart_item_path(cart_item.id), method: :delete, "data-confirm" => "Are you sure ?" %></p>
        </td>
        <% @total += cart_item.subtotal %>
      </tr>

    <% end %>